# Журнал действий

После подключения API каждое действие фиксируется с датой, платформой, типом
операции, затронутыми объектами, причиной и результатом. Секреты и персональные
данные лидов в журнал не записываются.
